# Normalize repository text files to LF to avoid Windows sync-folder churn.
* text=auto eol=lf

# Preserve native Windows endings for shell entrypoints that require them.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
